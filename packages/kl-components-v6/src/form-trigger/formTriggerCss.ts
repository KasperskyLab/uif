import styled from 'styled-components'
import { SPACES } from '../../design-system/theme/themes/variables'

export const TitleWrapper = styled.div`
  display: flex;
  flex-direction: column;
  gap: ${SPACES[3]}px;
`

export const ButtonVariantWrapper = styled.div`
  display: flex;
  flex-direction: row;
  gap: ${SPACES[12]}px;
`
