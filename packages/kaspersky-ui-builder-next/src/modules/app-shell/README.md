# App Shell / Layout

Собирает header, вкладки и трёхколоночный редактор.

`App.tsx` связывает модули; `workspace.ts` открывает стартовые формы.

Не хранит реализации API, истории, каталога форм или команд дерева.
