# Inspector

Редактирует локальный черновик настроек формы или выделенного элемента.

`Inspector.tsx` содержит поля props, значений, зависимостей, стилей и валидации.

Получает схему и registry через props; применяет черновик через callbacks сохранения. Не записывает форму напрямую.
