# Schema Model

Содержит общие типы формы и диагностик для остальных модулей.

`form-schema.ts` — контракт схемы v1; `diagnostics.ts` — тип Diagnostic.

Не зависит от UI, store, сети или конкретного runtime. Контракт должен соответствовать schemas/v1/form.schema.json.
