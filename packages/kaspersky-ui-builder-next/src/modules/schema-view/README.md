# Schema View

Отображает дерево формы и режимы «Схема», «JSON», «Превью».

`SchemaView.tsx` — дерево, диагностики и JSON; `FormPreview.tsx` — адаптеры Hexa UI, загружаемые лениво.

Изменения передаются через callbacks. Preview не исполняет произвольные пользовательские функции.
