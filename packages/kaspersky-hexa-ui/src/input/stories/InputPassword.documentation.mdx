import { Description, Meta } from '@storybook/addon-docs/blocks'
import { AutodocArgsTable, buildPresentationOnlyRows, DocExampleBlock, DocPageHeader, DocPageLayout, DocumentationArgsTable, DocumentationTabs as Tabs } from '@sb/components/Documentation'
import { Space } from '@src/space'
import { Text } from '@src/typography'

import { InputPassword } from '../InputPassword'
import {
  inputPasswordDesignPropPresentation,
  inputPasswordItemDesignPropPresentation
} from './InputPassword.design.properties'
import { inputPasswordDocumentationScope, inputPasswordActionsSetupCode, inputPasswordValidationRulesSetupCode } from './inputPasswordDocScope'
import * as InputPasswordStories from './InputPassword.stories'

<Meta of={InputPasswordStories} name="Input Password Docs" />

<DocPageLayout>

<DocPageHeader
  title={'Input Password'}
  description={'**Input Password** — поле ввода пароля с маскировкой символов, переключением видимости и опциональными правилами валидации'}
/>

<Tabs defaultActiveKey="description">
  <Tabs.TabPane tab="Описание" key="description">

    <DocExampleBlock
      title="Описание компонента"
      description="**Input Password** предназначен для безопасного ввода пароля: символы маскируются, пользователь может временно показать значение через иконку видимости. Реализован на Ant Design Input.Password с темизацией Hexa UI. Доступны **placeholder**, состояния **disabled** и **readOnly**, **validationStatus**, режим **clearBeforeFirstChange**, управление иконкой видимости через **showVisibilityIcon**, всплывающий список правил через **validationRules** и дополнительные кнопки через **actions**. Для обычного текстового поля используйте **Input**."
    />

    <DocExampleBlock
      title="value"
      description="Контролируемое значение поля — строка с введённым паролем. Изменения передаются через **onChange**."
      scope={inputPasswordDocumentationScope}
      code={`<InputPassword value="demoValue" onChange={(nextValue) => alert(nextValue)} />` /* not_a_secret */}
    />

    <DocExampleBlock
      title="onChange"
      description="Вызывается при изменении значения в поле. Передаёт актуальную строку пароля."
      scope={inputPasswordDocumentationScope}
      code={`<InputPassword onChange={(nextValue) => alert(nextValue)} />` /* not_a_secret */}
    />

    <DocExampleBlock
      title="placeholder"
      description="Подсказка в пустом поле, когда значение не задано."
      scope={inputPasswordDocumentationScope}
      code={`<InputPassword placeholder="Введите пароль" />` /* not_a_secret */}
    />

    <DocExampleBlock
      title="disabled"
      description="Отключает поле: ввод недоступен, иконка видимости не отображается. Доступные значения: `true`, `false`. По умолчанию — `false`."
      scope={inputPasswordDocumentationScope}
      code={`<InputPassword disabled placeholder="Password" />` /* not_a_secret */}
    />

    <DocExampleBlock
      title="readOnly"
      description="Режим только для чтения: значение видно, но редактирование недоступно. Доступные значения: `true`, `false`. По умолчанию — `false`."
      scope={inputPasswordDocumentationScope}
      code={`<InputPassword readOnly value="readonly-value" />` /* not_a_secret */}
    />

    <DocExampleBlock
      title="validationStatus"
      description="Визуальный статус валидации поля ввода. Доступные значения: `default`, `error`, `success`. По умолчанию — `default`."
      scope={inputPasswordDocumentationScope}
      code={`<InputPassword validationStatus="default" />
<InputPassword validationStatus="error" />
<InputPassword validationStatus="success" />`}
    />

    <DocExampleBlock
      title="clearBeforeFirstChange"
      description="При `true` при первом фокусе поле очищается до первого ввода пользователя — удобно при смене существующего пароля. После первого изменения значение отображается как обычно. Доступные значения: `true`, `false`. По умолчанию — `false`."
      scope={inputPasswordDocumentationScope}
      code={`<InputPassword clearBeforeFirstChange value="previousValue" />` /* not_a_secret */}
    />

    <DocExampleBlock
      title="showVisibilityIcon"
      description="Управляет отображением иконки переключения видимости пароля. При `disabled`=`true` иконка скрывается независимо от значения пропа. Доступные значения: `true`, `false`. По умолчанию — `true`."
      scope={inputPasswordDocumentationScope}
      code={`<InputPassword showVisibilityIcon={false} value="demoValue" />` /* not_a_secret */}
    />

    <DocExampleBlock
      title="validationRules"
      description="Массив правил валидации с полями **message** (текст правила) и **isValid** (выполнено ли правило). При непустом массиве при фокусе на поле открывается всплывающая панель со списком правил и индикаторами выполнения."
      scope={inputPasswordDocumentationScope}
      setupCode={inputPasswordValidationRulesSetupCode}
      code={`<InputPassword validationRules={rules} />`}
    />

    <DocExampleBlock
      title="actions"
      description="Дополнительные кнопки справа от поля. Каждый элемент — объект с **tooltip**, **icon** (имя иконки или React-элемент), необязательным **mode** кнопки и **onClick**."
      scope={inputPasswordDocumentationScope}
      setupCode={inputPasswordActionsSetupCode}
      code={`<InputPassword actions={actions} />`}
    />

    <DocExampleBlock
      title="onFocus и onBlur"
      description="Обработчики получения и потери фокуса полем ввода. Используются вместе с **clearBeforeFirstChange** для управления отображением значения."
      scope={inputPasswordDocumentationScope}
      code={`<InputPassword
  onFocus={() => alert('focus')}
  onBlur={() => alert('blur')}
/>`}
    />

  </Tabs.TabPane>

  <Tabs.TabPane tab="Правила использования" key="howtouse">

    <DocExampleBlock
      title="Правила использования"
      description="**Input Password** применяют в формах регистрации, смены пароля и аутентификации, когда нужно ввести или обновить секретное значение. Маскировка символов снижает риск случайного просмотра пароля посторонними; иконка видимости помогает проверить ввод без постоянного отображения текста.

Для сопровождения сложных политик пароля используйте **validationRules** вместе с **Field** и сообщением об ошибке. Дополнительные операции (копирование, генерация) оформляйте через **actions**, не перегружая основное поле. Для обычного текста без маскировки выбирайте **Input**."
    />

  </Tabs.TabPane>

  <Tabs.TabPane tab="Дизайнеру" key="design">
    <Space direction="vertical" gap={40} align="start">
      <Space direction="vertical" gap={16} align="start">
        <Text type="H3">input password</Text>
        <DocumentationArgsTable rows={buildPresentationOnlyRows(inputPasswordDesignPropPresentation)} />
      </Space>

      <Space direction="vertical" gap={16} align="start">
        <Text type="H3">{'input password item' /* not_a_secret */}</Text>
        <DocumentationArgsTable rows={buildPresentationOnlyRows(inputPasswordItemDesignPropPresentation)} />
      </Space>
    </Space>
  </Tabs.TabPane>

  <Tabs.TabPane tab="Properties" key="properties">
    <Description of={InputPasswordStories} />
    <AutodocArgsTable components={InputPassword} />
  </Tabs.TabPane>
</Tabs>

</DocPageLayout>
