import { ArgTypes, Description, Meta } from '@storybook/addon-docs/blocks'
import { AutodocArgsTable, buildPresentationOnlyRows, DocExampleBlock, DocPageHeader, DocPageLayout, DocumentationArgsTable, PropsTable, DocumentationTabs as Tabs } from '@sb/components/Documentation'
import { Text } from '@src/typography'

import { Badge } from '../Badge'
import * as BadgeStories from './Badge.stories'
import { badgeDesignPropPresentation } from './Badge.design.properties'

<Meta of={BadgeStories} name="Badge Docs" />

<DocPageLayout>

<DocPageHeader checkUIF title="Badge" />

<Tabs>
  <Tabs.TabPane tab="Описание" key="description">
<DocExampleBlock
      description="**Badge** — компактная метка для количества, статуса или короткой подписи рядом с элементом интерфейса: иконкой меню, вкладкой, строкой таблицы, аватаром. Используйте для вторичной информации, которая не требует отдельного блока контента.

Для важных сообщений на уровне страницы или раздела используйте **Alert**, а не **Badge**."
    />
    <DocExampleBlock
  title="Счётчик уведомлений"
  description="Используется для отображения количества уведомлений, сообщений или объектов рядом с иконкой или элементом управления.

`overflowCount` ограничивает максимальное отображаемое значение (например, +99)."
  previewGap={24}
  codeBlock="hidden"
  code={`
<Space gap={24}>
  <Badge count={1} />
  <Badge count={12} />
  <Badge count={105} overflowCount={99} />
</Space>`}
/>
<DocExampleBlock
  title="Текстовая метка / статус"
  description="Если задан **text**, Badge отображается как короткая текстовая метка или статус.

**text** имеет приоритет над **count** — при наличии текста счётчик не отображается."
  previewGap={24}
  codeBlock="hidden"
  code={`
<Space gap={16}>
  <Badge text="NEW" mode="new" />
  <Badge text="UPDATE" mode="update" />
  <Badge text="INFO" mode="info" />
</Space>`}
/>
<DocExampleBlock
  title="Размер large только для transparent"
  description="**size=large** поддерживается только для `neutralTransparent` и `criticalTransparent`.

Для остальных режимов используется **size=medium**."
  previewGap={24}
  codeBlock="hidden"
  code={`
<Space gap={16}>
  <Badge mode="neutral_transparent" size="large" text="Label" />
  <Badge mode="critical_transparent" size="large" text="Warning" />
</Space>`}
/>
<DocExampleBlock
  title="Семантика mode"
  description="Выбирайте `mode` в соответствии со смыслом события:

`positive`, `resolved` — успешные состояния

`ritical`, `inIncident` — ошибки и инциденты

`low`, `medium`, `high` — приоритеты

`info`, `new`, `update` — нейтральные уведомления и изменения

Не используйте `critical` как декоративный акцент — это сигнал ошибки или тревоги."
  previewGap={16}
  codeBlock="hidden"
  code={`
<Space gap={16}>
  <Badge mode="positive" count={1} />
  <Badge mode="critical" count={1} />
  <Badge mode="high" count={1} />
  <Badge mode="info" count={1} />
</Space>`}
/>
<DocExampleBlock
  title="Badge внутри других компонентов"
  description="Badge может использоваться рядом с текстом, иконкой или элементом навигации для отображения количества уведомлений или дополнительного статуса.

Часто применяется в tab navigation, списках и navigation item."
  previewGap={8}
  codeBlock="hidden"
  code={`
<Tabs defaultActiveKey="messages">
  <Tabs.TabPane
    tab={
      <span>
        Сообщения
      </span>
    }
    key="messages"
  />

  <Tabs.TabPane
    tab={
      <span>
        Задачи <Badge count={12} mode="accent" />
      </span>
    }
    key="tasks"
  />

  <Tabs.TabPane
    tab="Архив"
    key="archive"
  />
</Tabs>`}
/>

  </Tabs.TabPane>
  <Tabs.TabPane tab="Структура" key="composition">

    <DocExampleBlock
      
      description="**Badge** отображает число (**count**) или текст (**text**): уведомления, статусы, метки. Настраиваются **mode**, **size**, **overflowCount**, **showZero**, **theme** и **className**. При **count**=0 badge скрывается, если не задан **showZero**."
    />

    <DocExampleBlock
      title="mode"
      description="Семантический цветовой режим. Доступные значения: `neutral`, `info`, `new`, `update`, `in-progress`, `resolved`, `positive`, `accent`, `low`, `medium`, `high`, `in-incident`, `critical`, `neutral_transparent`, `critical_transparent`. По умолчанию — `neutral`. Неподдерживаемое значение заменяется на `neutral`, в консоль выводится предупреждение."
      code='
    <Badge mode="neutral" text="neutral" />
    <Badge mode="info" text="info" />
    <Badge mode="new" text="new" />
    <Badge mode="update" text="update" />
    <Badge mode="in-progress" text="in-progress" />
    <Badge mode="resolved" text="resolved" />
    <Badge mode="positive" text="positive" />
    <Badge mode="accent" text="accent" />
    <Badge mode="low" text="low" />
    <Badge mode="medium" text="medium" />
    <Badge mode="high" text="high" />
    <Badge mode="in-incident" text="in-incident" />
    <Badge mode="critical" text="critical" />
    <Badge mode="neutral_transparent" text="neutral_transparent" size="large" />
    <Badge mode="critical_transparent" text="critical_transparent" size="large" />'
    />

    <DocExampleBlock
      title="size"
      description="Размер badge. Доступные значения: `medium`, `large`. По умолчанию — `medium`. `large` допустим только для **mode** `neutral_transparent` и `critical_transparent`; для остальных режимов в типах задан `medium`. Если передан **size**=`large` без **mode**, используется `neutral_transparent`."
      code='
    <Badge mode="info" size="medium" text="medium" />
    <Badge mode="neutral_transparent" size="medium" text="medium" />
    <Badge mode="critical_transparent" size="medium" text="medium" />
    <Badge mode="neutral_transparent" size="large" text="large" />
    <Badge mode="critical_transparent" size="large" text="large" />'
    />

    <DocExampleBlock
      title="count"
      description="Число внутри badge. При **count**=0 badge скрывается, если **showZero** не задан. Игнорируется, если задан **text**."
      code='
    <Badge count={10} />
    <Badge count={0} />
    <Badge count={99} text="Text" />'
    />

    <DocExampleBlock
      title="text"
      description="Текстовая метка вместо числа. Имеет приоритет над **count**: при одновременной передаче отображается **text**."
      code='
    <Badge text="Badge text" />
    <Badge text="Badge text" count={99} />'
    />

    <DocExampleBlock
      title="overflowCount"
      description="Верхняя граница для **count**. По умолчанию — `99`. Если **count** больше **overflowCount**, отображается значение вида 99+ (с числом **overflowCount** и плюсом)."
      code='
    <Badge count={100} overflowCount={99} />
    <Badge count={1000} overflowCount={999} mode="critical" />'
    />

    <DocExampleBlock
      title="showZero"
      description="Показывает badge при **count**=0. Доступные значения: `true`, `false`. По умолчанию — `false`. Без **showZero** при нуле badge скрыт."
      code='
    <Badge count={0} />
    <Badge count={0} showZero />'
    />

    <DocExampleBlock
      title="title"
      description="Устаревший HTML-атрибут **title** для нативной подсказки при наведении. Для кастомной подсказки оберните badge в **Tooltip** — отдельного пропа tooltip у компонента нет."
      code={`
  <Tooltip text="3 new notifications">
    <span><Badge count={3} mode="info" /></span>
  </Tooltip>`}
    />

  </Tabs.TabPane>


  <Tabs.TabPane tab="Properties" key="properties">
    <AutodocArgsTable components={Badge}/>
  </Tabs.TabPane>

  <Tabs.TabPane tab="Дизайн" key="design">
    <DocExampleBlock
      title="Компонент в Pixso"
      titleSize='H5'
      description="[Открыть компонент в Pixso](https://pixso.net/app/design/_JQDYkYJW0yUZVsKeoSybw?item-id=53944:188107)"
    />

    <DocumentationArgsTable rows={buildPresentationOnlyRows(badgeDesignPropPresentation)} />
  </Tabs.TabPane>
</Tabs>

</DocPageLayout>
