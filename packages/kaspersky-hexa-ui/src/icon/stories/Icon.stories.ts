import { Icon as IconComponent } from './Icon'
import { sbIconColors } from './sbIconColors'

const meta = {
  title: 'Design/Icons',
  component: IconComponent,
  argTypes: {
    color: sbIconColors
  }
}

export default meta

export const Icon = {}
