import { ActionButton } from '@src/action-button'
import styled from 'styled-components'

export const Button = styled(ActionButton)`
  background: var(--code_compare--bg--controls) !important;
  width: 100% !important;
  padding: 6px 12px !important;
  gap: 4px !important;
`
