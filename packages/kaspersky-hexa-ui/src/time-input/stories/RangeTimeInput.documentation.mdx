import { Description, Meta } from '@storybook/addon-docs/blocks'
import {
  AutodocArgsTable,
  buildPresentationOnlyRows,
  ComponentChangelog,
  DocExampleBlock,
  DocPageHeader,
  DocPageLayout,
  DocumentationArgsTable,
  DocumentationTabs as Tabs,
} from '@sb/components/Documentation'
import { Text } from '@src/typography'

import { RangeTimeInput } from '../RangeTimeInput'
import { rangeTimeInputDesignPropPresentation } from './RangeTimeInput.design.properties'
import * as RangeTimeInputStories from './RangeTimeInput.stories'

<Meta of={RangeTimeInputStories} name="RangeTimeInput Docs" />

<DocPageLayout>

  <DocPageHeader title="RangeTimeInput" />

  <Tabs defaultActiveKey="description">

    <Tabs.TabPane tab="Описание" key="description">

      <DocExampleBlock
        description="
          **RangeTimeInput** — поле выбора интервала времени «с — по» с маской ввода

          **RangeTimeInput** применяют в формах и фильтрах, когда пользователю нужно указать интервал времени без привязки к дате: рабочие часы, временное окно, диапазон в расписании. Единый контрол с двумя полями и общим разделителем удобнее двух отдельных **TimeInput**.

          Для интервала дат с календарём используйте **RangePicker**. Для одного значения времени — **TimeInput**. Если нужна кастомная композиция с другим разделителем или промежуточными элементами, соберите два **TimeInput** вручную." />

    </Tabs.TabPane>

    <Tabs.TabPane tab="Структура" key="composition">

      <DocExampleBlock
        description="Публичный API выбора диапазона времени в Hexa UI — компонент **RangeTimeInput**. Состоит из двух полей **TimeInput** с разделителем «–». Поддерживает ввод с маской, **format**, **placeholder**, состояния **disabled** и **readOnly**, а также **validationStatus**. Для интервала дат с календарём используйте **RangePicker**. Для одного значения времени — **TimeInput**."
      />

      <DocExampleBlock
        title="valueStart / valueEnd"
        description="Контролируемые значения начала и конца интервала — строки в формате **format**. Изменения передаются через **onChangeStart** и **onChangeEnd**."
        code={`
          <RangeTimeInput valueStart="09:00" valueEnd="18:00" />
        `}
      />

      <DocExampleBlock
        title="onChangeStart / onChangeEnd"
        description="Вызываются при изменении времени в начальном или конечном поле соответственно. Каждый обработчик получает строковое значение времени и объект маски ввода."
        code={`
          <RangeTimeInput
            onChangeStart={(value) => console.log('Начало:', value)}
            onChangeEnd={(value) => console.log('Конец:', value)}
          />
        `}
      />

      <DocExampleBlock
        title="format"
        description="Формат отображения времени в обоих полях. Доступные значения: `HH:mm`, `HH:mm:ss`, `HH:mm:ss.ms`, `h:mm aaa`. По умолчанию — формат из дизайн-токенов."
        code={`
          <RangeTimeInput format="HH:mm" />
          <RangeTimeInput format="HH:mm:ss" />
          <RangeTimeInput format="h:mm aaa" />
        `}
      />

      <DocExampleBlock
        title="placeholder"
        description="Подсказка в пустых полях. Если не задана, формируется автоматически из **format** (например, `__:__` для `HH:mm`)."
        code={`
          <RangeTimeInput format="HH:mm:ss" placeholder="__:__:__" />
        `}
      />

      <DocExampleBlock
        title="disabled"
        description="Отключает оба поля: ввод недоступен. Доступные значения: `true`, `false`. По умолчанию — `false`."
        code={`
          <RangeTimeInput disabled />
        `}
      />

      <DocExampleBlock
        title="readOnly"
        description="Режим только для чтения: поля не редактируются. Доступные значения: `true`, `false`. По умолчанию — `false`."
        code={`
          <RangeTimeInput valueStart="09:00" valueEnd="18:00" readOnly />
        `}
      />

      <DocExampleBlock
        title="validationStatus"
        description="Визуальный статус валидации полей ввода. Доступные значения: `default`, `error`, `success`. По умолчанию — `default`."
        code={`
          <RangeTimeInput validationStatus="default" />
          <RangeTimeInput validationStatus="error" />
          <RangeTimeInput validationStatus="success" />
        `}
      />

    </Tabs.TabPane>

    <Tabs.TabPane tab="Properties" key="properties">

      <Description of={RangeTimeInputStories} />
      <AutodocArgsTable components={RangeTimeInput} />

    </Tabs.TabPane>

    <Tabs.TabPane tab="Дизайн" key="design">

      <DocExampleBlock
        title="Компонент в Pixso"
        titleSize="H5"
        description="[Открыть компонент в Pixso](https://pixso.net/app/design/_JQDYkYJW0yUZVsKeoSybw?item-id=80710:352613)"
      />
      <DocumentationArgsTable rows={buildPresentationOnlyRows(rangeTimeInputDesignPropPresentation)} />

    </Tabs.TabPane>

    <Tabs.TabPane tab="Изменения" key="changelog">

      <ComponentChangelog component="RangeTimeInput" />

    </Tabs.TabPane>

  </Tabs>

</DocPageLayout>
