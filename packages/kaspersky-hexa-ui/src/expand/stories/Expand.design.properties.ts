import type { PropPresentationMap } from '@sb/components/Documentation'

export const expandDesignPropPresentation: PropPresentationMap = {
  variant: { description: 'Вариант содержимого кнопки раскрытия', type: 'slot | text' }
}
