import { Description, Meta } from '@storybook/addon-docs/blocks'
import {
  AutodocArgsTable,
  buildPresentationOnlyRows,
  ComponentChangelog,
  DocExampleBlock,
  DocPageHeader,
  DocPageLayout,
  DocumentationArgsTable,
  DocumentationTabs as Tabs,
} from '@sb/components/Documentation'
import { Text } from '@src/typography'

import { Expand } from '../Expand'
import * as ExpandStories from './Expand.stories'
import { expandLongTextExampleCode } from './expandStoryData'
import { expandDesignPropPresentation } from './Expand.design.properties'

<Meta of={ExpandStories} name="Expand Docs" />

<DocPageLayout>

  <DocPageHeader title="Expand" />

  <Tabs defaultActiveKey="description">

    <Tabs.TabPane tab="Описание" key="description">

      <DocExampleBlock
        description="
          **Expand** — сворачивает длинный контент и показывает кнопку разворота, если текст не помещается в заданную высоту

          **Expand** уместен там, где длинный текст или описание нельзя показывать целиком без перегрузки экрана: карточка, боковая панель, модальное окно, ячейка или блок подробностей. Компонент даёт пользователю добровольно раскрыть фрагмент контента, не меняя структуру страницы.

          Для нескольких независимых секций с заголовками и разным содержимым используйте **Accordion**, а не **Expand**. Для основного действия на экране — **Button** или **Action Button**, а не подмену ими разворота текста."
      />

    </Tabs.TabPane>

    <Tabs.TabPane tab="Структура" key="composition">

      <DocExampleBlock
        description={'**Expand** оборачивает произвольное содержимое (**children**) и в свёрнутом виде ограничивает высоту контейнера через **collapsedHeight**. Если фактическая высота контента больше порога, под блоком появляется **Action Button** с подписью и иконкой стрелки; по клику контент разворачивается или снова сворачивается. Подписи кнопки настраиваются через **textToExpand** и **textToCollapse**.'}
      />

      <DocExampleBlock
        title="children"
        description="Содержимое внутри сворачиваемой области: текст, типографика, списки или другие компоненты Hexa UI."
        setupCode={expandLongTextExampleCode}
        code={`
          <Expand collapsedHeight={64}>{expandLongText}</Expand>
        `}
      />

      <DocExampleBlock
        title="collapsedHeight"
        description="Максимальная высота контейнера в свёрнутом состоянии (в пикселях, через `max-height`). Кнопка разворота показывается только если `scrollHeight` контента больше этого значения. По умолчанию — `64`."
        previewDirection="vertical"
        setupCode={expandLongTextExampleCode}
        code={`
          <Expand collapsedHeight={60}>{expandLongText}</Expand>
          <Expand collapsedHeight={60}>
            <Text>Если текст меньше collapsedHeight, то кнопка не показывается.</Text>
          </Expand>
        `}
      />

      <DocExampleBlock
        title="textToExpand и textToCollapse"
        description="Подписи **Action Button** в свёрнутом и развёрнутом состоянии. По умолчанию берутся из локализации: «Развернуть» и «Свернуть» (ключи `expand.toExpand`, `expand.toCollapse`)."
        setupCode={expandLongTextExampleCode}
        code={`
          <Expand
            textToExpand="Показать то, что скрыто"
            textToCollapse="Скрыть то, что показано"
          >
            {expandLongText}
          </Expand>
        `}
      />

    </Tabs.TabPane>

    <Tabs.TabPane tab="Properties" key="properties">

      <Description of={ExpandStories} />
      <AutodocArgsTable components={Expand} />

    </Tabs.TabPane>

    <Tabs.TabPane tab="Дизайн" key="design">

      <DocExampleBlock
        title="Компонент в Pixso"
        titleSize="H5"
        description="[Открыть компонент в Pixso](https://pixso.net/app/design/_JQDYkYJW0yUZVsKeoSybw?item-id=128727:472067)"
      />
      <DocumentationArgsTable rows={buildPresentationOnlyRows(expandDesignPropPresentation)} />

    </Tabs.TabPane>

    <Tabs.TabPane tab="Изменения" key="changelog">

      <ComponentChangelog component="Expand" />

    </Tabs.TabPane>

  </Tabs>

</DocPageLayout>
