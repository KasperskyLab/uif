import type { PropPresentationMap } from '@sb/components/Documentation'

export const terminalDesignPropPresentation: PropPresentationMap = {
  selection: {
    description: 'Подсветка выделенного фрагмента текста в терминале',
    type: 'Boolean',
    defaultValue: 'false'
  }
}
