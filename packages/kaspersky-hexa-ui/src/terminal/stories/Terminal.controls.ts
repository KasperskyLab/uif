import type { PropPresentationMap } from '@sb/components/Documentation'

export const defaultArgs = {}

// Терминал требует готовый dataTransferAddon, а options и customAddons — сложные объекты.
// Эти свойства не редактируются через Storybook Controls.
export const terminalPropPresentation: PropPresentationMap = {}
