import { Icon } from './Icon'
import { sbIconColors } from './sbIconColors'

const meta = {
  title: 'Icon/Icon',
  component: Icon,
  argTypes: {
    color: sbIconColors,
    opacity: { control: 'number' }
  }
}

export default meta

export const Color = {}
