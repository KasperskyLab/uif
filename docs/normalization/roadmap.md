# Roadmap: Normalization Tool

Краткий план работ. **Концепция, контракты и принятые решения** — в [tooling.md](./tooling.md) (границы продукта, паритет приложений, configHook, типизация, загрузка).

---

## Цель

Редактор и движок просмотра форм на базе **Hexa UI** и общего DSL. Ограничения и паритет **tool** / **viewer** — см. [tooling.md](./tooling.md#normalization-product-boundaries).

---

## ✅ 1. Конфиг-хуки (п.1)

**Готово:** единый контракт `configHook` и паритет **normalization-tool** / **transpile-viewer** для всех контролов скоупа.

**Скоуп контролов:** **Button**, **Text**, **Grid**, **Table** ([контракт в tooling.md](./tooling.md#normalization-config-hooks)).

**Прогресс**

- ✅ **Button** — [plans/plan-button.md](./plans/plan-button.md)
- ✅ **Text** — [plans/plan-text.md](./plans/plan-text.md)
- ✅ **Grid** — [plans/plan-grid.md](./plans/plan-grid.md)
- ✅ **Table** — [plans/plan-table.md](./plans/plan-table.md)

---

## 2. Типизация DSL и форматы форм (п.2)

Детали: [plans/plan-dsl-typing.md](./plans/plan-dsl-typing.md). Типизация и загрузка — также [tooling → типизация](./tooling.md#normalization-dsl-typing-loading).

**Принятые решения (на весь этап п.2)**

- **Общий код в монорепозитории:** типы, парсинг и нормализация DSL — в **одном общем модуле** в дереве репозитория; **normalization-tool** и **transpile-viewer** **импортируют** его (alias / путь сборки). **Отдельный пакет** с отдельным `package.json` для DSL **не вводится**, **без двух расходящихся копий** `form-dsl.ts`.
- **`FormSlice.state`:** остаётся **гибким** (например `Record<string, unknown>`), без обязательной жёсткой связки со `schema` на этом этапе.

### ✅ 2.1. Переход на TypeScript (формы и DSL)

**Готово.** Артефакты форм — **только `.ts`**; разбор и нормализация через **`parseFormTs`**, сохранение/экспорт — **`formToTs`**; публичные типы (**`FormData`**, схема полей) в **`shared/types/form.ts`**; ядро DSL в **`shared/normalization-form-dsl/`**; оба приложения подключают общий entry через alias **`@normalization/form-dsl`** (реэкспорт в пакетах при необходимости). Один shared-источник правды, без дубля полного `form-dsl`.

### ⏳ 2.2. Типизация и контракты динамических импортов

<a id="normalization-roadmap-p21"></a>
<a id="normalization-roadmap-p22"></a>

**Статус: отложено.** Уточнить типы и единые сигнатуры **динамической** подгрузки модулей (`import()` форм, **`configHook`**, прочие **`handlers`**), согласовать с бандлерами и песочницей. См. также [tooling → динамическая загрузка](./tooling.md#normalization-dsl-typing-loading).

---

## 3. Превью в редакторе

**План (целевая архитектура).** Одна библиотека **Hexa UI** для холста и превью; **расхождения рендереров между тулом и viewer не допускаются.**

- **Встраивание transpile-viewer в normalization-tool.** Движок **transpile-viewer** (в первую очередь **`FormRenderer`** и та же обвязка, что во внешнем приложении) **полностью занимает центральную область** редактора в режиме визуализации формы (вместо или поверх нынешнего отдельного **`FormPreview`**, решение по UX — на этапе внедрения). Пользователь видит форму **тем же кодом**, что и в **dev** (редактор) и **prod** (встроенный viewer, Storybook, отдельное приложение viewer).

- **Единообразие dev и prod.** Один **импортируемый контур** рендера DSL: тот же **entry-модуль** рендера (тот же кодовый путь импорта), те же правила `configHook`, те же ограничения песочницы загрузки модулей. Отдельное приложение **transpile-viewer** остаётся сценарием «только каталог + просмотр» без холста редактора, но **не дублирует** семантику рендера относительно встроенного режима в туле.

- **Сценарные моки и отдельный хэндлер.** Для проверки форм в превью без реального окружения (API, права, контекст продукта):
  - поддержка **сценарных моков** — фикстуры **`FormSlice`** / начального `state`, заглушки данных, флаги сценария;
  - в артефакте формы (или в настройках превью редактора) — **отдельный путь к хэндлеру** (по духу **`handlers/*.ts`**, как у `configHook`), который редактор вызывает **только в режиме превью** и который возвращает моки / переопределения контекста;
  - **пример контракта (черновик):** в корне формы поле вроде **`previewScenarioHook`:** `'handlers/preview-scenario.ts'`; модуль по умолчанию экспортирует функцию `(ctx: { formSlice: FormSlice, scenarioId?: string }) => { initialStatePatch?: Record<string, unknown>, mockServices?: … } | null` — точная сигнатура и имя поля фиксируются при реализации; в **`jsons/handlers/`** — образец **`preview-scenario.ts`** с демо-сценариями («пустая форма» / «с заполненными полями» / «ошибка валидации»).

**Открытые вопросы** (см. также реализацию п.3):

- ❓ **Grid и Table: `rows` / `cols` и configHook.** Сейчас в DSL явно задаются `rows` и `cols`, а на холсте normalization-tool **и сетка, и таблица** в основном строятся по этим полям; хук влияет на оформление контейнера (**`<Grid />`** / **`<Table />`**), но не на размерность. Не должны ли **рендер холста/превью** в редакторе в какой-то мере опираться и на возврат хука (в т.ч. на согласованные с DSL размеры), при том что **в артефакте формы** строки и колонки по-прежнему хранятся в DSL?

- ❓ **Table: тулбар и выбор строк.** Контрол `table` рендерится как Hexa **`<Table />`**, но тулбар сейчас **дублируется по смыслу**: в DSL остаётся **`toolbar`** (инспектор + **статический превью** над таблицей), а нативный тулбар Hexa можно задать только из **configHook** (`ITableProps['toolbar']`); на холсте хук таблицы не подмешивается. **Открыто:** свести рендеринг тулбара к **одному контракту** — перенести конфиг/рендер в **config hook** (и при необходимости упростить или переопределить роль поля в DSL), **плюс** добавить **рабочий пример** хука с **выбранными строками** (`rowSelection`, связка с состоянием формы / `FormSlice`, чтобы элементы тулбара реагировали на выделение).

---

## 4. Полная поддержка компонентов ДС

- **Перечень компонентов** и настройки из **всех** экспортов `@kaspersky/hexa-ui` и их **`.d.ts`**, без ручных списков.
- **Кастомный React-компонент** — подключение из **файла** (импорт модуля), с теми же принципами безопасной загрузки и паритетом **редактор / рендерер**, что у configHook (детали реализации — на этапе работы над пунктом).

---

## 5. Storybook и прод

**Normalization tool** — аддон Storybook. **Transpile viewer** — общий **движок рендера** DSL для Storybook и прод-приложений.

---

## 6. Архитектура кода и нейминг

- **Убрать дублирование:** каждый тип контрола обрабатывается **максимально общими** механизмами; всё **частное** выносится на **слой параметризации** (дескрипторы, конфиг маппинга, типы пропсов), а не копипаста веток в рендерерах и превью.
- **Нейминг (выровнять):** текущие **normalization tool** / **KUNT** → целевое имя **`kant-editor`**; **transpile viewer** → **`kant-renderer`** (пакеты, документация, UI-строки — по мере закрытия этапа).

---

## 7. Совместимость с JSON UI Builder и слой адаптеров

- **`form-dsl` и артефакты форм** — сделать **обратно совместимыми** с **JSON UI Builder**: один и тот же контракт данных (или явный мост версий / нормализация при импорте), без расхождения семантики контролов и корня формы.
- **Transpile viewer** — пересадить на **адаптеры** и **Auto UI**: вьюер не содержит монолитного маппинга «все типы подряд», а опирается на слой адаптеров к общему рантайму.
- **Auto UI** — **полная миграция на React** (отказ от устаревшего стека в пользу React end-to-end в рамках этой линии работ).

Детали контрактов, границы адаптеров и план миграции Auto UI — фиксировать по мере старта этапа (отдельные заметки или планы в [plans/](./plans/), по аналогии с п.1).

---

## 8. UI самих инструментов: только Hexa UI и, в перспективе, DSL + рендерер

- **Оба инструмента** (**normalization-tool** и **transpile-viewer**) для **собственного интерфейса** — оболочка, настройки, экраны, панели — должны опираться **только на компоненты Hexa UI** из **`@kaspersky/hexa-ui`**, без произвольной вёрстки «мимо» дизайн-системы там, где есть подходящий компонент ДС.
- **Целевое состояние:** инструменты **рисуют свой UI из DSL** и общего **рендерера** (тот же контракт «конфиг → дерево контролов → Hexa»), то есть **dogfooding**: редактор и просмотрщик не живут на отдельном ad-hoc UI-слое, а переиспользуют тот же движок, что и пользовательские формы (с оговорками по спец-режимам вроде холста/DnD — детали на этапе проектирования).
