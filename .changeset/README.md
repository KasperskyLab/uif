# Changesets

Changeset files in this directory are **auto-generated** from conventional commits by the custom script `scripts/create-changeset-from-commit.js` (run in CI or optionally as a commit-msg hook). Do not add manual changeset files unless your workflow uses manual `npx changeset`; see [MONOREPO-SCHEME.md](../docs/MONOREPO-SCHEME.md).
